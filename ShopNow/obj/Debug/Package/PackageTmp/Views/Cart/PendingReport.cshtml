@model IEnumerable<ShopNow.ViewModels.CartReportViewModel.CartReportList>
@using GridMvc.Html
@{
    ViewBag.Title = "Pending Order Report";
}
<title>ShopNow | Pending Report </title>
<link href="~/Content/Gridmvc.css" rel="stylesheet" />
<script src="~/Scripts/gridmvc.js"></script>
@using (Html.BeginForm("PendingReport", "Cart", FormMethod.Post, new { id = "PendingReportForm", role = "form", enctype = "multipart/form-data" }))
{
    <div class="container-fluid">
        <div class="d-print-none">
            <div class="row my-2">
                <div class="col-md-11">
                    <h5 class="text-success text-nowrap">Pending Report</h5>
                </div>
                <!--
    <div class="col-md-2">
        <div class="mb-1 data-search">
        </div>
    </div>
    <div class="col-md-2">
        <select name="ShopCode" id="ShopCode" class="form-control">
            if (ViewBag.ShopCode != null || ViewBag.ShopCode != "")
            {
                <option value="ViewBag.ShopCode">ViewBag.ShopName</option>
            }
        </select>
        <input type="hidden" name="ShopName" id="ShopName" value="ViewBag.ShopName" />
    </div>

    <div class="col-md-4">
        <div class="input-daterange input-group">
            <input type="date" class="form-control well well-sm" placeholder="From Date" id="datepicker" name="startDate">
            <span class="input-group-addon mx-3">to</span>
            <input type="date" class="form-control" placeholder="To Date" id="datepicker1" name="endDate">
            <input type="hidden" id="StartDate" value="ViewBag.StartingDate" />
            <input type="hidden" id="EndDate" value="ViewBag.EndingDate" />
        </div>
    </div>
    <div class="col-md-1">
        <input type="submit" class="btn btn-sm btn-info" value="Apply Filter" />
    </div>
        -->
                <div class="col-md-1 text-right">
                    <div class="input-group">
                        <button id="btnPrint" class="btn btn-sm btn-secondary mr-2" value="Print"><i class="fa fa-print fa-lg" aria-hidden="true"></i></button>
                        <button id="btnExport" class="btn btn-sm btn-secondary" value="Export To Excel"><i class="fa fa-file-excel-o fa-lg" aria-hidden="true"></i></button>
                    </div>
                </div>
            </div>
        </div>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-12">
                        <h4 class="text-center d-none d-print-block">Pending Report</h4>
                        @Html.Grid(Model).Columns(columns =>
{
columns.Add(c => c.OrderNo).Titled("OrderNo").Filterable(true).SetWidth(10);
columns.Add(c => c.ShopName).Titled("Shop Name").Filterable(true).SetWidth(15);
columns.Add(c => c.DeliveryAddress).Titled("Delivery Address").Filterable(true).SetWidth(15);
columns.Add(c => c.PhoneNumber).Titled("Phone").Filterable(true).SetWidth(15);
columns.Add().RenderValueAs(c => c.CartStatus == 2 ? "Pending" :"N/A").Titled("Order Status").Filterable(true).SetWidth(10);
columns.Add(c => c.DateEncoded).Titled("Date").Filterable(true).SetWidth(10);
}).WithPaging(20).Sortable(true)

                        <!--
                        <table class="table table-striped table-hover table-borderless" id="Pending-list">
                            <thead>
                                <tr>
                                    <th class="text-theme ">No.</th>
                                    <th class="text-theme text-nowrap">Order No.</th>
                                    <th class="text-theme text-nowrap">Shop Name</th>
                                    <th class="text-theme text-nowrap">Delivery Address</th>
                                    <th class="text-theme">Phone</th>
                                    <th class="text-theme">Status</th>
                                    <th class="text-theme">Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                foreach (var item in Model.List)
                                {
                                <tr>
                                    <td></td>
                                    <td>item.OrderNo</td>
                                    <td>item.ShopName</td>
                                    <td>item.DeliveryAddress</td>
                                    <td>item.PhoneNumber</td>
                                    <td><span class="text-danger">Pending</span></td>
                                    <td class="text-nowrap">item.DateEncoded.ToString("dd MMMM yyyy")</td>
                                </tr>
                                }
                            </tbody>
                        </table>
                        -->
                    </div>
                </div>
            </div>
        </div>
    </div>
}
@section Scripts
{
    <!--Select2-->
    <script src="~/Scripts/select2/js/select2.js"></script>
    <link href="~/Scripts/select2/css/select2.css" rel="stylesheet" />
    <link href="~/Content/select2/select2.custom.css" rel="stylesheet" />
    <!--Select2 End-->
    <script src="~/Scripts/exporttoexcel/jquery.table2excel.js"></script>
    <script src="~/Scripts/datepicker.js"></script>
    <script>
        $(document).ready(function () {
            var startdate = $("#StartDate").val();
            var enddate = $("#EndDate").val();
            if (startdate != "") {
                var arr = startdate.split('/');
                var fromdate = arr[0] + "/" + arr[1] + "/" + arr[2];
                // $('#datepicker').val(fromdate);
                $('#datepicker').attr('value', fromdate);
            }
            if (enddate != "") {
                var arr = enddate.split('/');
                var todate = arr[0] + "/" + arr[1] + "/" + arr[2];
                // $('#datepicker1').val(todate);
                $('#datepicker1').attr('value', todate);
            }
        
            $('#btnPrint').click(function () {
                $('.container-fluid *').removeClass('card');
                $('.container-fluid *').removeClass('card-body');
                //$('.dataTables_info').addClass('d-none');
                //$('.dataTables_paginate').addClass('d-none');
                window.print();
            });

            $("#btnExport").click(function () {
                $("#Pending-list").table2excel({
                    name: "Worksheet Name",
                    filename: "PendingOrderReport",
                    fileext: ".xls",
                    exclude_links: true,
                });
            });

            $('#ShopCode').select2({
                placeholder: "Search Shop",
                ajax: {
                    url: "/Cart/GetShopSelect2",
                    width: '100%',
                    delay: 250,
                    dataType: 'json'
                }
            }).on('select2:select', function (e) {
                $('input[name="ShopName"]').val(e.params.data.text);
            });

            $("#Filter").click(function () {
                var shopcode = $("#ShopCode").val();
                window.location.href = "/Cart/PendingReport/?shopcode=" + shopcode;
            });


        });

    </script>
}

