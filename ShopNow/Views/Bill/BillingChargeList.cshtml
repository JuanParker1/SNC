@*@model ShopNow.ViewModels.BillListViewModel*@
@model IEnumerable<ShopNow.ViewModels.BillListViewModel.BillList>
@using GridMvc.Html
@{
    ViewBag.Title = "Billing Charge List";
}
<title>ShopNow | Billing Charge List</title>
<link href="~/Content/Gridmvc.css" rel="stylesheet" />
<script src="~/Scripts/gridmvc.js"></script>

<div class="container-fluid">
    <div class="row my-2">
        <div class="col-md-6">
            <h5 class="text-success" style="letter-spacing:1px">Billing Charge Setting List</h5>
        </div>        
        <div class="col-md-6 text-right">
            <a href="~/Bill/BillingCharge" target="_self"><span class="fa fa-plus"></span> Entry of Billing Charge Setting</a>
        </div>
    </div>
    @Html.Grid(Model).Columns(columns =>
{
    columns.Add(c => c.ShopName).Titled("Shop Name").Filterable(true).SetWidth(10);
    columns.Add(c => c.ConvenientCharge).Titled("Convenient Charge").Filterable(true).SetWidth(15);
    columns.Add(c => c.ItemTypeText).Titled("Packing Type").Filterable(true).SetWidth(10);
    columns.Add(c => c.PackingCharge).Titled("Packing Charge").Filterable(true).SetWidth(10);
    columns.Add()
    .Encoded(false)
    .Sanitized(false)
    .RenderValueAs(o => Html.ActionLink("Edit", "BillingUpdate", new { code = @ShopNow.Helpers.AdminHelpers.ECode(o.Code) }, new { @class = "btn btn-sm btn-warning mr-1", @style = "padding: 3px 26px" }).ToHtmlString()
                   + Html.ActionLink("Delete", "BillingDelete", new { code = @ShopNow.Helpers.AdminHelpers.ECode(o.Code) }, new { @class = "btn  btn-sm btn-danger", @style = "padding: 3px 18px" }).ToHtmlString()).Titled("Action").SetWidth(15);
}).WithPaging(20).Sortable(true)


    <!--
    <div class="row">
        <div class="col-md-12">
            <div class="card mb-3">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover table-borderless table-hover" id="billingcharge-list">
                            <thead>
                                <tr>
                                    <th>No.</th>
                                    <th>Shop Name</th>
                                    <th>Convenient Charge Range</th>
                                    <th>Packing Type</th>
                                    <th>Packing Charge</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                if (Model.List != null)
                                {
                                foreach (var item in Model.List)
                                {
                                <tr>
                                    <td></td>
                                    <td>item.ShopName</td>
                                    <td>item.ConvenientCharge</td>
                                    f (item.ItemType == 0)
                                    {
                                    <td>Single Item</td>}
                                    else if (item.ItemType == 1)
                                    {
                                    <td>Total Item</td>}
                                    else
                                    {
                                    <td>N/A</td>}
                                    <td>item.PackingCharge</td>
                                    <td class="text-break">
                                        <a href="~/Bill/BillingUpdate?code=ShopNow.Helpers.AdminHelpers.ECode(item.Code)" class="btn btn-warning text-center btn-sm text-nowrap" style="padding: 3px 26px">Edit</a>
                                        <a href="~/Bill/BillingDelete?code=ShopNow.Helpers.AdminHelpers.ECode(item.Code)" class="btn btn-danger text-center btn-sm text-nowrap" style="padding: 3px 18px">Delete</a>
                                    </td>
                                </tr>
                                }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    -->
</div>

@section Scripts
{
    <script>
        $(document).ready(function () {
            var t = $('#billingcharge-list').DataTable({
                "dom": "<'row'<'col-sm-6'f>>" +
                    "<'row'<'col-sm-5'i><'col-sm-3'l><'col-sm-4'p>>" +
                    "<'row'<'col-sm-12'tr>>",
                "bPaginate": false,
                "scrollY": 400,
                "paging": true,
                "lengthMenu": [[10, 20, 50, 100, -1], [10, 20, 50, 100, "ALL"]],
                "pageLength": 10,
                initComplete: function (settings, json) {
                    $('.index input').remove();
                    $('.action-btn input').remove();
                    $('.dataTables_filter input').appendTo('.data-search');
                    $('.dataTables_filter label').remove();
                    $('.data-search input').attr('placeholder', 'Quick Search');
                    $('.data-search input').addClass('form-control');
                    $('.data-search input').removeClass('form-control-sm');
                },
            });

            t.on('order.dt search.dt', function () {
                t.column(0, { search: 'applied', order: 'applied' }).nodes().each(function (cell, i) {
                    cell.innerHTML = i + 1;
                });
            }).draw();

        });

    </script>
}