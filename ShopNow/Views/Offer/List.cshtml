@model ShopNow.ViewModels.OfferListViewModel
@using GridMvc.Html
@{
    ViewBag.Title = "Offers List";
}

<title>ShopNow | @ViewBag.Title</title>
<link href="~/Content/Gridmvc.css" rel="stylesheet" />
<script src="~/Scripts/gridmvc.js"></script>


<div class="container-fluid">
    <div class="row">
        <div class="col-md-6 mx-auto mt-3">
            <div class="card shadow">
                <div class="card-body">
                    <h5 class="text-success" style="letter-spacing:1px">@ViewBag.Title</h5>
                    @Html.Grid(Model.ListItems).Columns(columns =>
               {
                   columns.Add(c => c.OwnerTypeText).Titled("Owner").Filterable(false).SetWidth(10);
                   columns.Add(c => c.Name).Titled("Name").Filterable(false).SetWidth(10);
                   columns.Add(c => c.TypeText).Titled("Type").Filterable(false).SetWidth(10);
                   columns.Add(c => c.DiscountTypeText).Titled("Discount Type").Filterable(false).SetWidth(10);
                   columns.Add(c => c.Percentage).Titled("Percentage Type").Filterable(false).SetWidth(10);
                   columns.Add()
        .Encoded(false)
        .Sanitized(false)
        .RenderValueAs(o => Html.ActionLink("View", "Edit", new { id = @ShopNow.Helpers.AdminHelpers.ECodeInt(o.Id) }, new { @class = "btn btn-vsm btn-info mr-1", }).ToHtmlString()
        + Html.ActionLink("Delete", "Delete", new { id = @ShopNow.Helpers.AdminHelpers.ECodeInt(o.Id) }, new { @class = "btn btn-vsm btn-secondary mr-1", }).ToHtmlString()).Titled("Action").SetWidth(15);
               }).Sortable(false)
                </div>
            </div>
        </div>
    </div>
</div>
